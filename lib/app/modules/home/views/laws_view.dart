import 'package:flutter/material.dart';

class LawsView extends StatelessWidget {
  const LawsView({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "ច្បាប់ និងលិខិតបទដ្ឋាន",
        style: TextStyle(fontSize: 20, color: Colors.white),
      ),
    );
  }
}
