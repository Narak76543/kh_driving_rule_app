import 'package:flutter/material.dart';

class TrafficSignViews extends StatelessWidget {
  const TrafficSignViews({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("ផ្លាកសញ្ញាចចរាចរណ៍ ស្ថិតក្នុងការ អភិវឌ្ឍន៍",style: TextStyle(fontSize: 25, fontWeight: FontWeight.bold),),
      ),
    );
  }
}
