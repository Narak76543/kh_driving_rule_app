import 'package:flutter/material.dart';

class TransportView extends StatelessWidget {
  const TransportView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("ការផ្ទុកមនុស្សនិងទំនិញ ស្ថិតក្នុងការ អភិវឌ្ឍន៍",style: TextStyle(fontSize: 25, fontWeight: FontWeight.bold),),
      ),
    );
  }
}
