import 'package:flutter/material.dart';

class GeneralKnowLedgeView extends StatelessWidget {
  const GeneralKnowLedgeView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("ចំណេះដឹងទូទៅ ស្ថិតក្នុងការ អភិវឌ្ឍន៍",style: TextStyle(fontSize: 25, fontWeight: FontWeight.bold),),
      ),
    );
  }
}
